<div class="authform row">
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {class: 'd-flex flex-column justify-content-center col-lg-6 mx-auto card p-5 my-5', :role => 'form'}) do |f| %>
    <h3>Ingresá</h3>

    <div class="form-group">
        <%= f.label :email, "Email *" %>
        <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= f.label :password, "Contraseña *" %>
        <%= f.password_field :password, class: 'form-control' %>
    </div>
    <% if flash[:alert] %>
    <div class="alert alert-danger" role="alert">
        <p class="my-0"><%= flash[:alert] %></p>
    </div>
    <% end %>
    <%= f.submit 'Ingresá', :class => 'btn btn-primary' %>
    <div class="d-flex justify-content-between my-2">
        <% if devise_mapping.rememberable? -%>
        <div class="form-check" style="width:150px">
            <label class="form-check-label">
                <%= f.check_box :remember_me, class: "form-check-input"%> Recordarme
            </label>
        </div>
        <% end -%>
        <span class="d-flex flex-column align-items-end">
            <%- if devise_mapping.recoverable? %>
            <%= link_to "Olvidaste tu contraseña?", new_password_path(resource_name), class: 'right' %>
            <% end -%>
            <%- if devise_mapping.registerable? %>
            <%= link_to 'Registrate', new_registration_path(resource_name), class: 'right'  %>
            <% end -%>
        </span>
        <% end %>
    </div>
</div>